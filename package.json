{
  "name": "coremind",
  "version": "0.1.0",
  "private": true,
  "description": "Trusted internal knowledge Q&A prototype with source tracing and governance controls.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
